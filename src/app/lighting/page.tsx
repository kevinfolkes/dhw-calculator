import LightingCalculator from "@/components/LightingCalculator";

/**
 * `/lighting` — multifamily lighting calculator route. Mounts the
 * LightingCalculator component below the AppShell brand bar.
 */
export default function LightingPage() {
  return <LightingCalculator />;
}
